<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto_app_moviles">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ver_Usuarios"></activity>
        <activity android:name=".Registrar_usuario" />
        <activity android:name=".detalle_producto" />
        <activity android:name=".ver_producto" />
        <activity android:name=".agregar_productos_tienda" />
        <activity android:name=".agregar_producto_carrito" />
        <activity android:name=".productos" />
        <activity android:name=".inicio" />
        <activity android:name=".carrito" />
        <activity android:name=".inicio_sesion" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>